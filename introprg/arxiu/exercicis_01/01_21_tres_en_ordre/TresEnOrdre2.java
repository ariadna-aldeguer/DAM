/* Programa que et retorna tres en ordre*/

public class TresEnOrdre2 {
    public static void main(String[] args) {
    	System.out.println("Primer?");
    	int a = Integer.parseInt(Entrada.readLine()); 
   	System.out.println("Segon?");
   	int b = Integer.parseInt(Entrada.readLine()); 
   	System.out.println("Tercer?");
   	int c = Integer.parseInt(Entrada.readLine()); 
   	int tmp = 0;
   	
   	if (a > b){
   	tmp = a;
   	a = b;
   	b = tmp;
   		if (med > gra) {
   		tmp = med;
   		med = gra;
   		gra = tmp;
   		System.out.println(peq + ", " + med + " i " + gra);
   		}
   	}
   }
}		
   	
